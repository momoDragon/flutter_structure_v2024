# flutter_structure_v2024

for testing:
flutter test --coverage
genhtml coverage/lcov.info -o coverage/html
open coverage/html/index.html

for build runner:
flutter pub run build_runner build --delete-conflicting-outputs

